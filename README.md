# paper-aeroplane-autopilot (individual report)


This repository contains the MATLAB script used to generate the figures and numerical checks in the individual report.

## Files
- `run_induvidual_autopilot_range.m` — runs the baseline vs autopilot comparison, gain sweep, implementation checks, and robustness check.
